- content_for :body_class, "projects_index"

.content
  .content-container
    .h3like.page-title
      Open Source
    .h5like
      These are some of the open source projects I'm currently working on or have worked on in the past. You should be able to find all of these on my 
      = link_to "Github", "#{github_url}"
      \. 
      %br
    - current_projects.each do |p|
      .project
        .project-header.text-center
          .h2like
            #{p.title.capitalize}

          %p.date
            = p.start_date.strftime("%B %d, %Y").upcase
            \-
            - if p.end_date.present?
              = p.end_date.strftime("%B %d, %Y").upcase
            - else 
              PRESENT
          .h5like
            = link_to specific_project_path(p.title.downcase), :class => "c2a" do
              Read more about #{p.title.capitalize} 
              %span
                %small
                  >

        .project-desc
          %p
            = p.slug
          %p
            - if p.url.present?
              %span
                = link_to "#{p.url}", "#{p.url}", :class => "c2a", :target => "_blank"
          %p.technologies-used
            = p.technologies
      .hr