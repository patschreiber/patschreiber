.container-content
  .row
    .one
      .personal-blurb
        .profile-img
        %p
          Lives in Chicago. Lives for the web.<br> I enjoy building elegant solutions to complex problems, predominantly using http.
  .row
    .one
      .showcase
        .item.item-colorized-0
          %a{:href => open_source_index_path}
            %span.text
              Open Source
        .item.item-colorized-1
          %a{:href => "#"}
            %span.text
              Professional
        .item.item-colorized-2
          %a{:href => "#{github_url}", :target => "_blank", :rel => :noopener}
            %span.text
              Github
        .item.item-colorized-0
          %a{:href => contact_path}
            %span.text
              Contact
        .item.item-colorized-1
          %a{:href => "#"}
            %span.text
              Blog
        .item.item-colorized-2
          %a{:href => "#"}
            %span.text
              API
  .row
    .one
      %p
        Show 3 newest blog posts here. Add "Read More" link under that directs to the blog page (or AJAX loads, whatever). If blog post is too long, truncate the post.

  .row
    .one-half
      .box.floating
        %p
          Lorem ipsum dolor  <small>This is a small</small> Lorem ipsum dolor  Lorem ipsum dolor  Lorem ipsum dolor  Lorem ipsum dolor  Lorem ipsum dolor

  .row
    - @blog.each do |p|
      %h3{:id => "post-" + p.id.to_s}
        = p.title
      %p
        #{ raw($redcarpet_markdown.render( p.post )) }
      %p
        = p.initial_posting.strftime( "%B %d, %Y" )


